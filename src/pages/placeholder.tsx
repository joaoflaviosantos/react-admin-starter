import { Typography } from 'antd';

export default function PlaceholderPage() {
  return (
    <div className="min-h-screen bg-ant-bg-layout p-12">
      <Typography.Title level={2}>Boilerplate</Typography.Title>
      <Typography.Paragraph>
        React Ant Design Admin Boilerplate. Login, layout, and CRUD land in later epics.
      </Typography.Paragraph>
    </div>
  );
}
